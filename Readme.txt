This is a maven project.

hence, import this project as  maven project.

ensure that all dependencies in pom file Are sucessfully downloaded by running a "verify install"

Using BDD cucumber feature files, run the test directing from the feature file,

Also Ensure all necessary plugins are installed, such as gherkin, and cucumber plugins

IDE of choice is Intellij

Run a maven build and run the test.
